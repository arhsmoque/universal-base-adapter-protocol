# Sandbox Adapter

Sandbox adapters isolate generated code, shell execution, network access, and broad filesystem access. Require timeout, allowlist, scoped writable directories, no inherited secrets, and audit records.
