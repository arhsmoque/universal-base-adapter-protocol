# Scriptable Path Recipes

Store active recipes that make repeated edits or similar tool-development paths cheap and replayable.

Use `templates/SCRIPTABLE_PATH_RECIPE.md` for new recipes. Keep this directory indexed from `AGENTS.md` or component metadata. Remove stale drafts during housekeeping.
